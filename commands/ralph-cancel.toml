description = "Cancel an active Ralph loop."
prompt = """
Cancel the active Ralph loop by running:
```bash
python -m ralph_loop.cli cancel
```

This will set `active: false` in the state file at `.gemini/ralph-loop.local.md`.

After cancellation, report the final status to the user including:
- Total iterations completed
- What was accomplished
- Any remaining work
"""
